name = "arc-inbound-relayer"
main = "src/index.js"
compatibility_date = "2025-01-01"

[triggers]
crons = ["*/2 * * * *"]   # Run every 2 minutes

[[kv_namespaces]]
binding = "ARC_INBOUND_KV"
id = "REPLACE_WITH_YOUR_KV_ID"

[vars]
# ARC_RELAYER_PRIVATE_KEY = "0x..."   # Add this as a secret in dashboard instead of here
