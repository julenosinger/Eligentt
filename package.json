{
  "name": "elligentt",
  "version": "5.0.0",
  "private": true,
  "description": "Elligentt — Batch Payments, Swap & Bridge on Arc Mainnet",
  "scripts": {
    "build": "node scripts/build.js",
    "dev": "vite",
    "sync": "node scripts/verify-shared-sync.cjs",
    "deploy": "node scripts/build.js && npx wrangler pages deploy public/"
  },
  "devDependencies": {
    "terser": "^5.49.2",
    "vite": "^6.0.0",
    "vitest": "^3.2.7",
    "wrangler": "^3.0.0"
  },
  "dependencies": {
    "@circle-fin/adapter-viem-v2": "^1.18.0",
    "@circle-fin/bridge-kit": "^1.15.1",
    "ethers": "^6.13.5",
    "pdfkit": "^0.19.1",
    "viem": "^2.56.8"
  }
}
